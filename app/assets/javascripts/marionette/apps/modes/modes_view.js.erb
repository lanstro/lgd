Lgd.module('Modes', function(Modes, Lgd, Backbone, Marionette, $, _){
	Modes.View = Marionette.ItemView.extend({
		template: JST['marionette/templates/modes'],
		
		events: {
			"click p": "alertClick"
		},
		
		alertClick: function(){
			alert("clicked");
		}
	});
});