Lgd.module('Modes', function(Modes, Lgd, Backbone, Marionette, $, _){
	Modes.Controller = {
		initialize: function(){
			var modesView  = new Modes.View();
			Lgd.ModesRegion.show(modesView);
		}
	};
});