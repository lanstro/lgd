<% provide(:title, 'Create new legislation') %>
<h1>Create new legislation</h1>

<div class="row">
	<%= form_for(@act) do |f| %>
		<%= render 'form', f: f, errors: @act.errors %>
		<%= f.submit "Create new act", class: "btn btn-large btn-primary" %>
	<% end %>
</div>