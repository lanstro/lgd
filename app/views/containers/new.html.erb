<% provide(:title, 'Create new container') %>
<h1>Create new container</h1>

<div class="row">
	<%= form_for(@container) do |f| %>
		<%= render 'form', f: f, errors: @container.errors %>
		<%= f.submit "Create new container", class: "btn btn-large btn-primary" %>
	<% end %>
</div>